[build]
  functions = "functions"

# The web frontend lives on GitHub Pages (jpdias.me/periphery); Netlify hosts
# only the API functions. Rewrite API calls to the Netlify functions.
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
