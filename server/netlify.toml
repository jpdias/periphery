[build]
  publish = "../web/public"
  functions = "functions"

# Serve the SPA and rewrite API calls to the Netlify functions.
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
